package com.example.backend.domaine;

/** Source d'une présence : marquée par l'étudiant ou ajoutée manuellement par le formateur (RG11). */
public enum SourcePresence {
    ETUDIANT,
    FORMATEUR
}
